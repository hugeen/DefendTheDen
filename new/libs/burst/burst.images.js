define([
    'jquery',
    'underscore',
    'caman'
], function($, _, Caman) {
    
    return {
        manipulateImage: function(image) {
            
            //Caman("#", function() {});
            //return canvas.toDataURL();
            
        }
    };

});
