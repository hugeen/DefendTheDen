define([
    'Underscore',
    'Backbone'
], function(_, Backbone) {
    
    var Level = Backbone.Model.extend({
        defaults: {},
        initialize: function() {
            
        }
    });
    
    return Level;
    
});
