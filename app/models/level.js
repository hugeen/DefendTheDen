define([
    'Underscore',
    'Backbone'
], function(_, Backbone) {
    
    var LevelModel = Backbone.Model.extend({
        defaults: {},
        initialize: function() {
            
        }
    });
    
    return LevelModel;
    
});
